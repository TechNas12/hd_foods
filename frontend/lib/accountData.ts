// TypeScript interfaces for account data
// Actual data comes from API calls — no more mock data

export type OrderStatus = 'Processing' | 'Shipped' | 'Out for Delivery' | 'Delivered' | 'Cancelled';
